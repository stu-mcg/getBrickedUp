
<!DOCTYPE html>
<style>
    html, body {
    min-height: 100%; 
    justify-content: center;
    }
    body, div, form, input, select, p { 
    padding: 0;
    margin: 0;
    outline: none;
    font-family: Roboto, Arial, sans-serif;
    font-size: 16px;
    color: #eee;
    }

    @keyframes gradient {
    0% {
    background-position: 0% 50%;
    }
    50% {
    background-position: 100% 50%;
    }
    100% {
    background-position: 0% 50%;
    }
    }

    body {
        background: linear-gradient(-45deg, #ee7752, #bd2323, #e73c7e, #23a6d5, #23d5ab);
        background-size: 400% 400%;
        animation: gradient 5s ease infinite;
    }
</style>

<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title> </title>
    </head>
    <body>
        <h1 align="center">Let's get BRICKED UP</h1>
        <form align="center">
        <a class = "button" href="/login">Login</a>
        <a class = "button" href="/listprod">Begin Shopping</a>
        <a class = "button" href="/listorder">List All Orders</a>
        <a class = "button" href="/customer">Customer Info</a>
        <a class = "button" href="/admin">Administrators</a>
        <a class = "button" href="/logout">Log out</a>
        </form>
        {{#if username}}
            <h3 align="center">Signed in as: {{username}}</h3>
        {{/if}}
      </body>
    </html>
</html>
